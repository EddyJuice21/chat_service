# Chat Service API

Тестовое задание для позиции Python Developer.
Асинхронный REST API для управления чатами и сообщениями. Реализован на базе FastAPI и PostgreSQL.

## 🛠 Технологии
* **Python 3.11**
* **FastAPI**
* **PostgreSQL** (драйвер `asyncpg`)
* **SQLAlchemy 2.0** (ORM)
* **Alembic** (миграции БД)
* **Docker & Docker Compose**

## ✅ Функционал
* Создание и удаление чатов.
* Отправка сообщений в чат.
* Получение истории сообщений (с пагинацией/лимитом).
* Каскадное удаление сообщений при удалении чата.
* Валидация данных (Pydantic).
* Автоматическая документация (Swagger UI).

## 🚀 Инструкция по запуску

Для запуска вам понадобится установленный Docker и Docker Compose.

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/EddyJuice21/chat_service
   cd chat_service
2. **Запустите проект:**

    ```bash
    docker-compose up --build

3. **Примените миграции (инициализация БД): Откройте новый терминал в папке проекта и выполните команду:**

    ```bash
    docker-compose exec web alembic upgrade head
   
4. **Готово! Документация API (Swagger) доступна по адресу:**

👉 http://localhost:8000/docs

